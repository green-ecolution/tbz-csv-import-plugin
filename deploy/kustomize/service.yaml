# https://kubernetes.io/docs/concepts/services-networking/service/
apiVersion: v1
kind: Service
metadata:
  name: csv-import
spec:
  selector:
    app: csv-import-plugin
  type: ClusterIP
  ports:
  - name: csv-import 
    protocol: TCP
    port: 80
    targetPort: http

